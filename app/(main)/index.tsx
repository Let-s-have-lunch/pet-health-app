import { Text } from "react-native";

function HomeScreen() {
    // 홈화면에 나와야 할것
    // 1. 강아지 등록 카드
    // 2. 대쉬보드처럼 백엔드 서비스에서 산책, 음수, 몸무게, 병원기록을 한꺼번에 받아와서
    // 하나의 스테이트에 저장해서 화면에 카드형식으로 뿌려주는 코드가 와야합니다.
    return <Text>Initialization</Text>;
}

export default HomeScreen;
